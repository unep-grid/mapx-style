  # .env.development  ← committed to git, loaded automatically by `npm run dev`
  # Override the default production asset URL for local development.
  # Requires the MapX dev API to be running locally.
  #VITE_MAPX_ASSET_BASE_URL=http://apidev.mapx.localhost:8880/s3
